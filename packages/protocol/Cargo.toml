[package]
name = "protocol"
version = "0.0.1"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
ts-rs = { version = "12.0.1", features = ["serde-json-impl", "serde-compat"] }

[features]
multi-tenant = []
