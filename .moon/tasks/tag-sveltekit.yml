# Only apply to projects tagged 'sveltekit'. In moon v1, the tag-X.yml filename
# scoped automatically; v2 requires explicit `inheritedBy`.
inheritedBy:
  tags: 'sveltekit'

extends: 'https://raw.githubusercontent.com/moonrepo/moon-configs/master/javascript/sveltekit/tasks.yml'
