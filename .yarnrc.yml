approvedGitRepositories:
  - "**"

enableGlobalCache: false

enableScripts: true

injectEnvironmentFiles:
  - .env.yarn?

npmAuthToken: "${NPM_AUTH_TOKEN:-fake-token}"

yarnPath: .yarn/releases/yarn-4.14.1.cjs
